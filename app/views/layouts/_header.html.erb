<nav class="navbar navbar-inverse navbar-satatic-top">

  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#expanded-menu" aria-expanded=" false">
        <span class="sr-only">Togle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Contact List', root_url, class: 'navbar-brand' %>

    </div>
    <!-- div: navbar-header -->

    <div class="collapse navbar-collapse" id="expanded-menu">
      <ul class="nav navbar-nav">
        <% if !user_signed_in? %>
          <li class="active">
            <%= link_to 'Nova Conta', new_user_path %>
          </li>
        <% end %>

      </ul>

      <ul class="nav navbar-nav navbar-right">

        <% if !user_signed_in? %>
          <li>
            <%= link_to 'Entrar', entrar_path %>
          </li>
        <% else %>
          <li class="dropdown">

            <a href="#" class="dropdown" data-toggle="dropdown" role="button" aria-haspopup="">
              Bem Vindo, <%= current_user.name %> <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to 'Minha Pagina', current_user %>
              </li>
              <li>
                <%= link_to 'Editar', '#' %>
              </li>
              <li class="divider" role="separator"></li>
              <li>
                <%= link_to 'Sair', sair_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% end %>

      </ul>

    </div>
    <!-- div: collapse navbar-collapse -->

  </div>
  <!-- div: container-fluid -->

</nav>
